name: Webhook on Event
# Make sure to change what triggers your action based on your needs
on: [milestone, create, delete, fork, issues, member, pull_request, pull_request_review, issue_comment, push]
jobs:
  # ID of our job
  webhook-request:
    # OS to run our job on
    runs-on: ubuntu-latest
    steps:
    - name: Send a request to webhook
      run: |
        RESPONSE=$(curl -H "Content-Type: application/json" -X POST -d '{
        "username": "GitHub",
        "content": "@Boy",
        "embeds": [
        {
        title: '🚀 ทดสอบการส่ง Embed',
        description: 'นี่เป็นการทดสอบการส่ง embed ไปยัง Discord จากระบบ AI Code Review & Deployment Monitoring',
        color: 5763719, // สีเขียว
        fields: [
          {
            name: '📂 โปรเจ็ค',
            value: 'ai-review-improve',
            inline: true
          },
          {
            name: '🌿 Branch',
            value: 'main',
            inline: true
          },
          {
            name: '👨‍💻 ผู้ Deploy',
            value: github.actor,
            inline: true
          },
          {
            name: '📝 Commit Message',
            value: 'ทดสอบระบบแจ้งเตือน Discord',
            inline: false
          }
        ],
        footer: {
          text: 'AI Code Review System',
          icon_url: 'https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png'
        }
        }
        ];  
        }' ${{ secrets.DISCORD_WEBHOOK_URL }})
        echo "Webhook server response: $RESPONSE"
